{
  "name": "multi-tenant-saas-api",
  "version": "1.1.0",
  "description": "Multi-tenant SaaS API with per-tenant MongoDB database isolation",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=20"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.0.1",
    "mongoose": "^8.9.5"
  }
}
